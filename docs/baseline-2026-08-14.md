# Live-day marker — 2026-08-14

Written the day 1.0.6 went `READY_FOR_SALE`, so the reposition has a dated
boundary. Pair with [`baseline-2026-08-01.md`](baseline-2026-08-01.md).

**Do not treat the 2026-08-01 numbers as a before/after for this listing.**
That capture was for App Store ID `6780682022`, which no longer resolves on
iTunes Search. Today's live app is a **new** listing.

## What is live right now

| | |
|---|---|
| App Store ID | **6798461672** (`DayCast Weather`) |
| Bundle | `com.scubasteve1999.DayCast` |
| Version | **1.0.6** · build **122** |
| State | `READY_FOR_SALE` / `READY_FOR_DISTRIBUTION` / `downloadable=true` |
| `currentVersionReleaseDate` | **2026-08-14T10:06:10Z** |
| Public `releaseDate` | **2026-08-14T07:00:00Z** |
| Listing URL | https://apps.apple.com/us/app/daycast-weather/id6798461672 |
| Subtitle (live) | `Storm Spotter Radar & SPC` |
| Keywords (live) | `spotter,chaser,chasing,nexrad,velocity,srv,supercell,mesocyclone,hail,severe,tornado,outlook,nws` |
| Promotional text (live) | Matches `ASO-1.0.6-REPOSITION.md` |
| Description (live) | Spotter-niche copy from `ASO-1.0.6-REPOSITION.md` |
| Public ratings | **0** (iTunes lookup + ASC `customerReviews` both empty) |
| DayCast Pro Monthly | `APPROVED` (`com.scubasteve1999.DayCast.pro.monthly`) |

Gate clock starts **2026-08-14**. Earliest 30-day trip: **2026-09-13**.
Second trip: 10+ subscribers.

## Analytics views — not captured

The two dashboard views from the 2026-08-01 baseline
(Acquisition → Overview, Acquisition → Sources, Product Page Views by Unique
Devices) **were not pulled today**.

Cause: the App Manager API key (`ZCMMSMJLQD`) returns `403 FORBIDDEN` on
`/v1/apps/{id}/analyticsReportRequests`. Apple requires an Admin key to
request Analytics Reports the first time; Sales and Reports can download
after that. There is no App Store Connect API for the live dashboard
numbers the baseline used.

**Today's impressions / page views / downloads / search-source split are
unknown.** Record them by hand before they get mixed with later days:

1. [App Store Connect → DayCast Weather → Analytics → Acquisition → Overview](https://appstoreconnect.apple.com/analytics)
2. Same section → **Sources**, metric **Product Page Views (Unique Devices)**
3. Paste the funnel table + source-type rows into this file under
   "Dashboard capture" below.

Watch **App Store Search impressions** first. Browse and Referrer are not
what the keyword/subtitle change moves.

Because this is a new listing, expect structurally zero or near-zero for
a few days. That is not a failed reposition — Apple has not had time to
index the new keywords against this record.

## Dashboard capture

*Empty on purpose. Fill from the two views above.*

| Stage | Count | Notes |
|---|---|---|
| Impressions | | |
| of which App Store Search | | the metric that decides the gate |
| Product page views | | |
| First-time downloads | | |
| Redownloads | | |
| Conversion rate | | ignore until volume exists |
| Ratings | 0 public via iTunes | confirm in Overview |
| Paying users / IAP | | |
| Subscribers | | 10+ trips the gate |

Range used: ______________ (write the exact ASC date pill).

Source types with any signal: ______________

## Listing-ID split (do not paper over)

| ID | Status on 2026-08-14 |
|---|---|
| `6780682022` | Dead. iTunes lookup returns 0 results. This is the ID in the 2026-08-01 baseline, `THINKING-PASS-2026-08.md`, `ShareAttribution.appStoreID`, and `AppReviewPrompt` review URLs. |
| `6798461672` | Live. 1.0.6 launched on this record today. |

Share campaign links and the in-app "write a review" URL still point at
the dead ID. That is a separate, small fix — it does not trip the
Spotter Network gate, but it does mean share attribution cannot work
until the ID is updated. The `pt=128792554` provider token may also
belong to the old record; confirm in ASC → Analytics → Acquisition →
Campaigns before changing it.

## Repo mismatch (not blocking)

`project.yml` still has `CURRENT_PROJECT_VERSION: "120"`. The live
binary is build 122 (bumped only in `project.pbxproj` at `30963fd`).
The next `xcodegen generate` will reset the project file to 120.
Bump `project.yml` to 122 (or higher) before the next archive.

## How this capture was taken

- App Store Connect API, App Manager key, 2026-08-14.
- Confirmed: version state, attached build 122, live subtitle/keywords/
  description, subscription group, public iTunes lookup.
- Not available with this key: Analytics Reports, Sales reports,
  subscriber count.
