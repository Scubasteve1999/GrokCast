{
  "name": "daycast-refs-agreement",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Point JSON for DayCast REFS timing confidence. GRIB stays on the server.",
  "scripts": {
    "test": "node --test 'test/**/*.test.js'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
