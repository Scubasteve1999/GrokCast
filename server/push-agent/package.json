{
  "name": "spottercast-push-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Agents SDK worker that watches NWS alerts and delivers APNs pushes to DayCast devices.",
  "scripts": {
    "test": "node --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "agents": "^0.20.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "@types/node": "^26.1.2",
    "typescript": "^5.9.0",
    "wrangler": "^4.118.0"
  },
  "engines": {
    "node": ">=24"
  }
}
